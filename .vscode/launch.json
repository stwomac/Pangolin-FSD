{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/pangolin-app/pangolin/package.json",
                "localRoot": "${workspaceFolder}/pangolin-app/pangolin"
            }
        }
    ]
}